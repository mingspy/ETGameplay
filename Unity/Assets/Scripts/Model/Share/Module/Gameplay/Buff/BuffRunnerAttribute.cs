using System;

namespace ET
{
    /// <summary>
    /// Buff处理系统
    /// </summary>
    public class BuffRunnerAttribute: BaseAttribute
    {
        /*
        public Type BuffClassType { get; } 
        public BuffRunnerAttribute(Type buffClassType)
        {
            if(!buffClassType.IsSubclassOf(typeof(BuffDataBase)))
            {
                throw new ArgumentException();
            }
            this.BuffClassType = buffClassType;
        }
        */
    }
}